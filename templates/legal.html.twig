{% extends 'partials/base.html.twig' %}

{% block content %}
<article class="standard-page legal-page">
  <h1>{{ page.title }}</h1>
  <div class="legal-intro">{{ page.content|raw }}</div>
</article>
{% endblock %}
