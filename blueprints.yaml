name: Pool Pilot
slug: poolpilot
type: theme
version: 1.0.0
description: Thème officiel du site pool-pilot.app
icon: tint
author:
  name: amery74
homepage: https://pool-pilot.app
keywords: grav, home assistant, hacs, pool pilot
license: MIT
dependencies:
  - { name: grav, version: '>=2.0.0' }

form:
  validation: loose
  fields:
    enabled:
      type: toggle
      label: Thème activé
      highlight: 1
      default: 1
      options:
        1: Oui
        0: Non
      validate:
        type: bool
