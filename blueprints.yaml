name: Pool Pilot
slug: poolpilot
type: theme
version: 0.2.0
description: Thème officiel du site Pool Pilot
icon: tint
author:
  name: amery74
homepage: https://pool-pilot.app
keywords: grav, pool pilot, home assistant, hacs
license: MIT
dependencies:
  - { name: grav, version: '>=1.7.0' }

form:
  validation: loose
  fields:
    enabled:
      type: toggle
      label: Thème activé
      highlight: 1
      default: 1
      options:
        1: Oui
        0: Non
      validate:
        type: bool
